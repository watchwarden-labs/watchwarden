{
	"name": "watchwarden",
	"private": true,
	"workspaces": [
		"packages/*",
		"controller",
		"ui"
	],
	"scripts": {
		"build:packages": "npm -w @watchwarden/types run build && npm -w @watchwarden/sdk run build",
		"build": "npm run build:packages && npm -w watchwarden-controller run build && npm -w ui run build",
		"test": "npm -w watchwarden-controller run test && npm -w ui run test",
		"type-check": "npm -w @watchwarden/types run build && npm -w @watchwarden/sdk run build && npm -w watchwarden-controller run type-check && npm -w ui run type-check",
		"docker": "docker compose build && docker compose up",
		"docker:d": "npm run docker:build && npm run docker:up",
		"docker:build": "docker compose build",
		"docker:up": "docker compose up -d",
		"docker:logs": "docker compose logs -f"
	}
}